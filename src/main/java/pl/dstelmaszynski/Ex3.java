package pl.dstelmaszynski;

public class Ex3 {
    public static int sum(int x, int y) {
        int result = x + y;
        System.out.println("\n" + x + "+" + y + "=" + result);
        return (x + y);
    }
}
